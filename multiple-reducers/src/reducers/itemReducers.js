const initialItemState = {

    items: [
        'Meth',
        'flask',
        'organic Chemicals'
    ]
}

export default function itemReducers(state = initialItemState, action) {

    return state
}