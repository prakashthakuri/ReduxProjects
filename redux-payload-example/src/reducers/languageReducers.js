
// This is the store
const initialData = {
    languagename : 'JavaScript'
}

// store is defined as a state in reducer function
export default function LanguageReducers(state = initialData) {

    return state
}